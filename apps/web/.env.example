# =================================
# ZYPHON WEB - VERCEL ENVIRONMENT VARIABLES
# =================================
# Copy these to your Vercel project settings → Environment Variables
# Set for all environments: Production, Preview, Development

# ─── REQUIRED ───────────────────────────────────────────────

# Database (NeonDB Postgres)
# Use the POOLER url for Vercel serverless functions
# Format: postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/db?sslmode=require
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Direct DB URL for migrations (non-pooled, used by Prisma migrate)
# DIRECT_DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# ─── RECOMMENDED ────────────────────────────────────────────

# LLM Configuration (required for AI task execution)
# Must be accessible from Vercel's network (not localhost!)
# Options: cloud Ollama, ngrok tunnel, or hosted LLM service
OLLAMA_BASE_URL="https://your-ollama-host.example.com"
OLLAMA_MODEL="deepseek-coder-v2:16b"

# Cron job secret (protects /api/cron/process-tasks endpoint)
# Generate with: openssl rand -hex 32
CRON_SECRET=""

# ─── OPTIONAL ───────────────────────────────────────────────

# Public API URL (if using separate backend server)
# NEXT_PUBLIC_API_URL="https://api.yourdomain.com"

# API URL for server-side calls
# API_URL="https://api.yourdomain.com"

# Redis (for BullMQ task queue - use Upstash for serverless)
# REDIS_URL="redis://default:pass@host:6379"

# Image Generation (optional - requires Python + GPU)
# SD3_SCRIPT_PATH=""
# SD3_MODEL_PATH=""

# LLM request timeout in ms (default: 120000)
# LLM_TIMEOUT_MS="120000"

# ─── AUTO-SET BY VERCEL ─────────────────────────────────────
# VERCEL=1
# VERCEL_REGION=iad1
# These are set automatically, do not override
