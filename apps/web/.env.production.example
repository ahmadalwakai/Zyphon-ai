# =================================
# ZYPHON WEB - PRODUCTION ENVIRONMENT
# =================================
# Copy to .env.production and fill in values
# .env.production is gitignored - do NOT commit secrets
#
# ─── VERCEL DEPLOYMENT ──────────────────────────────────────
#
# Set environment variables via Vercel Dashboard or CLI:
#
#   Dashboard: https://vercel.com → Project → Settings → Environment Variables
#   CLI:       vercel env add VARIABLE_NAME production
#
# IMPORTANT:
#   - REDIS_URL must NOT be localhost. Use Upstash (https://upstash.com)
#     or another cloud Redis provider accessible from Vercel's network.
#   - INTERNAL_API_URL must be a publicly reachable https:// endpoint
#     (e.g., https://api.yourdomain.com or your worker's public URL).
#   - Do NOT commit secrets to git. Use Vercel's encrypted env storage.
#
# ════════════════════════════════════════════════════════════

# ─── REQUIRED ───────────────────────────────────────────────

# Database (NeonDB Postgres - use POOLER url for serverless)
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# ─── TASK QUEUE ─────────────────────────────────────────────

# Redis for BullMQ (use Upstash for serverless)
REDIS_URL="redis://default:password@host:6379"

# ─── AI / LLM ───────────────────────────────────────────────

# Ollama endpoint (must be network-accessible, not localhost on Vercel)
OLLAMA_BASE_URL="https://your-ollama-host.example.com"
OLLAMA_MODEL="deepseek-coder-v2:16b"

# LLM request timeout (ms)
LLM_TIMEOUT_MS="300000"

# ─── IMAGE GENERATION (optional) ────────────────────────────

# SD3 paths (only for self-hosted, not Vercel)
SD3_SCRIPT_PATH=""
SD3_MODEL_PATH=""
PYTHON_BIN="python"

# ─── WORKSPACES ─────────────────────────────────────────────

# On Vercel this defaults to /tmp/workspaces
WORKSPACE_ROOT="./workspaces"

# ─── SECURITY ───────────────────────────────────────────────

# Cron job authentication
CRON_SECRET=""

# ─── EXTERNAL API (optional) ────────────────────────────────

# If using separate API server
NEXT_PUBLIC_API_URL=""
API_URL=""
INTERNAL_API_URL=""
